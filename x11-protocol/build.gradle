allprojects {
    group = 'com.github.moaxcp.x11.x11-protocol'
    repositories {
        mavenCentral()
    }
}

subprojects {
    apply plugin: 'io.freefair.lombok'

    delombok {
        modulePath = classpath
    }
}